# Primitive Extension Scaffolds

Status: Scaffold

This folder holds non-production scaffolds for extending the case study beyond the
baseline vault program.

These files are intentionally not wired into the workspace build. They are templates
to accelerate AI-assisted development while keeping the current demo stable.

Current scaffold:

- `escrow_mini/` - minimal primitive + Trident harness skeleton.
